import { Padding } from './padding';

export class BaseConfig {
	element: string;
	outerWrapperElement: string;
	time: number;
	padding: Padding;
	minBarHeight: number;
	maxBarHeight: number;
	estimatedTime: number;
}
